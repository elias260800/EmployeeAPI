# URL base de la API backend
VITE_API_BASE_URL=http://localhost:8080/api

# Modo de filtrado de empleados: 'local' (en memoria, recomendado para datos masivos) o 'server' (endpoint API con query params)
VITE_EMPLOYEE_FILTER_MODE=local
